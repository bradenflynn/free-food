# Plan: 01-01-PLAN.md (Supabase Schema Setup)

**Phase:** 1 (Cloud Foundation)
**Wave:** 1
**Autonomous**: No (Requires User to create Supabase project)

## Goal
Establish the cloud database structure in Supabase to receive event data.

## Requirements
- **DATA-01**: Migrated `food_events` table to Supabase PostgreSQL.

## Tasks
```xml
<task id="1">
  <description>User creates Supabase project at supabase.com and provides URL/Key.</description>
</task>
<task id="2">
  <description>Define the 'food_events' table in Supabase SQL Editor.</description>
</task>
<task id="3">
  <description>Enable Row Level Security (RLS) for public READ access.</description>
</task>
```

## Verification Criteria
- Table `food_events` exists in Supabase.
- Table columns match the SQLite schema (`source`, `source_handle`, `post_id`, `club_name`, `event_name`, `date`, `time`, `location`, `food_provided`, `has_free_food`, `food_rank`, `confidence_score`, `image_path`, `status`).

## must_haves
- Supabase project is active.
- Schema is compatible with existing Python logic.
